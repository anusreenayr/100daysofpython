#You can multiply a bunch of numbers to figure out how many seconds are in a year.
#Use input and if statements to add the extra day for leap year

seconds_per_minute = 60
minutes_per_hour = 60
hours_per_day = 24
days_per_year = 365

seconds_per_day = seconds_per_minute * minutes_per_hour * hours_per_day

seconds_per_year = seconds_per_minute * minutes_per_hour * hours_per_day * days_per_year
print("Seconds per year (non-leap year):", seconds_per_year)

leap_year = input("Is it a leap year? (yes/no): ")

if leap_year.lower() == "yes":
    seconds_per_year += seconds_per_day
    print("Seconds per year (leap year):", seconds_per_year)
else:
    print("Seconds per year (non-leap year):", seconds_per_year)
